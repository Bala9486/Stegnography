# Stegnography
A C-based LSB Steganography project that securely hides and extracts secret text messages in BMP images using bitwise operations. Implements encoding and decoding with efficient file handling while preserving image quality. Built using C, GCC, and Linux to demonstrate file I/O and image processing concepts.
